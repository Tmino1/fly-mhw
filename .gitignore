.venv/
__pycache__/
*.pyc
/tmp_capture/
*.state.json

# nix build output symlink, if ever used
result
result-*

# recorded demo episodes (frames + JSONL) — large per-episode dumps,
# same treatment as .venv/
/demos/storage/

# isolated worktrees for background subagents (e.g. the Phase 2
# connectome-brain agent) — separate workspaces, reviewed/merged
# manually, never committed directly here
/.claude/
